Module 1

things you know
-------------------
1. HTML(hypertext Markup Language) :- basic structure : HTML builds the skeleton of web pages by using tags to define elements like text, images, and links.

2. CSS(Cascading Syle Sheets) :- Design and Style : CSS controls how web pages look, allowing you to change colors, fonts, and layouts to make the site altractive.

3. JS(javascript) :- interactive features : JS makes web pages interactive, letting users do things like click buttons, fill out forms, and see changes without refreshing the page. 



What is React js
-------------------
1. react js is an open-source js library developed and maintained by facebook
2. it's widely used for building user interfaces, especially for single page applications and complex web interfaces.
3. it breaks down a whole website into small, manageable pieces called components
4. it enables developers to create dynamic, responsive, and reusable UI compnents, enchancing the efficiency of the development process



why React js
--------------
1. speed :- react uses of the virtual DOm aloows for efficient updates and rendering, resulting in faster and more responsive user interfaces
2. reusability :- React allows components based approach where the component can be reusable this leads to cleaner code, easier maintenance, and better collaboration among team members
3. job market :- it has high demand! companies like netflix, AirBnd, instagram etc.
4. Rich Ecosystem And Tooling :- react is supported by a vast ecosysytem of libraries, tools and frameworks that enhance its capabilities and help developers build robust applications more efficiently


About js
---------- 
1. it is a js library developed by FB in 2011, it has been released in 2013
2. it has been invented by jordon walke.
3. angular js and Vue js are the competitor for react
4. it has component-based architectur. where the component can be reusable which helps in maintaining and organizing the code efficiency
5. react uses a virtual DOM to optimize rendering, which improves performance
6. unidirectional data flow. it means data flows in a single direction, form parent components to child components 



Most Popular technology skills in the JS tech Stack
------------------------------------------------------
1. react
2. node
3. typescript
4. angular
5. ES6

current version of React JS is :- 19.2
--------------------------------

which language is using in React JS :- .JSX
---------------------------------

Single page Application(SPA)
------------------------------
a SPA in react is a web application where the entire website loads only once in the browser. after that, different pages or content are updated dynamically without refreshing the whole page.


what is the purpose of react js
---------------------------------
1. react is used to create user interface
2. react is used to create single page appliaction with other libraries.
3. using react we can create static and dynamic webpage


can we create entire frontend application using react?
--------------------------------------------------------
to create frontend application with react?
-----> UI + other libraraies (axios, hooks, react router, dom, react icons, redux)


Advantages of react JS
-----------------------
1. react us easy to learn and use
2. react learning curve is very small
3. react application are very fast in performance
4. in react we have virtual dom
5. the react has component based architecture
6. react community is larger


Working of React Application
-----------------------------




user interaction                   no server is sent to server          
     client ------------------------------------------------------------------------> server
1. if we want to navigate from one page to another page in react the application will not send any request to server.
2. the page will not refreshing in react. without refreshing it will navigate from one page to another page.
3. we will get request from react and react only take care of all request.
4. it is also called as single page application.
5. because it has the concept called virtual dom. lets see dom interaction.


what is DOM?
-------------
1. dom(document object model) is a tree like structure that represents your HTML elements in the browser.
<div>
    <h1>hello world</h1>
    <button>click me</button>
</div>

2. this becomes DOM tree like


what is the virtual DOM?
-------------------------
1. The virtual DOM is a copy of the real DOM kept in memory by react.
2. it's a JS object that mirrors the structure of  the real DOM. but doesn't touch the screen immediately.
react never updates the DOM directly: instead it
-------------------------------------------------
1. creates a virtual version of the UI



why is this important?
-----------------------
updating the real DOM is slow because
1. it involves recalculating styles, layouts and reflows
2. the entire page may be re-rendered unnecessarily.

react's virtual DOM makes updates faster and smoother by
----------------------------------------------------------
react creates a virtual dom in memory :- instead of a manipulating the browers's dom directly, React creates a virtual dom in memory


Difference between ANGULAR AND REACT :
---------------------------------------
                            ANGULAR                               REACT

     AUTHOR            |   Google                           |   Facebook
     Developer         |   Mishko Hevery                    |   Jordan Walke
     Latest Version    |   Angular 16.0.3 may 23 -2023      |   React 19.0.0 2025
     Language          |   JS,HTML                          |  JSX
     Rendering         |   Client side                      |   server side
     Data-binding      |   Bi-directional                   |   uni-directiona
     DOM               |   Regulat DOM                      |   Virtual DOM
     App Architecture  |   MVC (model,view,control)         |   Flux
     Routing           |   It required a template or        |   It doesn't handle routing , but
                       |   controller to its router         |   has a lot of module for routing
                       |  configuration , which has to      |  ex: react -router
                       |   be managed manually              |


              React.js                                      React Native

        Released in 2013                              |  released in 2015
        used to develop web application               |  develop mobile applications
        can execute on all platform                   |  it is not platform independent, (take more effort to execute on all  platform)
        it use js library and css for animantions     |  it comes with build-in animation libraries
        it use react router to navigate web page      |  has build-in navigation library for navigating mobile application
        use HTML tags                                 |  It doesn't use HTML tags
        provide security                              |  low compare to react.js 


   Working of React application
   ----------------------------

                      HTTP REQUEST
      |------------- |  ------------->   |----------|
      |   CLINET     |                   |  SERVER  |
      |------------- |  <------------    |----------|
                        HTTP RESPONSE

// Html file will 
// be downloaded in 
//  browser

//:                    no req send to server
//-   |-------------|  ------------->   | 
//-   |  CLINET     |                   |  SERVER
//-   |-------------|  <------------    |  
//+  www.facebook.com                 

// If we want to navigate from one page to another page in react the application will not send ant req to server
// The page will not refresh in react. without refreshing it will navigate from one page to another page
// we will get request from react and react to only take care of all request
// It is also called as single page application
// Because it has concept called virtual DOM      


//? what is DOM ?
// DOM(document Object Model) is a tree like structure that represent your html element in browser
//:  <div>
//:  <h1> hello </h1>
//:  <button> click here </button>
//:  </div>

//!  This become DOM tree
//*    div
//*     |--- h1
//*     |---- button

// ChANging or updating



//? What is virtual DOM ?
// The virtual DOM is a copy of real DOM kept in m/m by react.
// it's a js object that mirrors that structure of the real DOM .but doesn't touch the screen immeditely

// + REACT never updates the DOM directly : instead it
// create a virtual version of UI
// Update the virtual version when state or prope changes
// Compares it with the previous version
// finds what changed
// Efficientely updates only those are changed


// ? WHEN  is virtual dom used?

//:  client  ---->  virtual dom ----->  Original dom

//  everytime when something changed in your application 
//  re-renders the components to create a new virtual DOM to find what changes 
//  Calculate the minimum no. of changes required
// applies those changes to the real DOM


//? WHY IS THIS IMP?
//  UPDATING THE REAL DOM IS SLOW because
//  It involve recalculation styles, layout and reflows
//  The entire page may be re-rendered unnecessarily

//? React's virtual DOM makes update fast and smoother by
//  avoiding unnecessary work
//  Making dom updates batched and optimized 

// *             VIRTUAL DOM                                       ORIGINAL DOM

//-                  |                                                   | 
//-              DOCUMENT                                            DOCUMENT
//-                  |                                                   |
//-                 HTML                                                HTML
//-                  |                                                   |
//-          ----------------                                     ---------------
//-          |       |       |                                    |      |       |
//-          H1    BUTTON   H2                                   H1    BUTTON    H2


//? React created=d a virtual DOM
// Instead of a manipulating the browser DOM directly, react creates a virtual DOM in m/m. 
// Where it does all the necessary manipulating before the changes in the browser DOM.

//? React changes only what need to change in original DOM
// When user want to change anything it will reflect in virtual dom .
// The virtual DOM will update the changes which is need in original dom.


Features of react js
----------------------
1. JSX :- javascript XML is asyntax extension for js used in react. it allows you to write HTML-like code in your javascript components.

2. performace :- react js is known to be a great performer, 
this features makes it much better then other frameworks out ther today. the reason behind this is that it manages a virtual DOM.

3. simplicity :- react js uses jsx which makes the application simple and to code as well as undestand.
we know that react js is a component based approach which makes the code resuable as yours need.

4. virtual DOM :- A virtual Dom is a copy of original dom. whenewver any modification happen in the web application the entire UI is rerenderes in virtual dom representation.
then it checks the difference b/w dom representation and new dom only it updates if it needed

5. one-way data binding :- react js is designed in such a manner that follows unidirectional data flow or one way data binding.
the benefits of one way data binding give you better control throughout the application.

6. components :- React js is all about components. React.js application is made up of multiple components and each components has its own logic and controls. these components can be reusable.


why react is used to create UI when we can create UI using html and js??
-------------------------------------------------------------------------
1. HTML :- 
ui----> html----> pre defined element
using html creating ui is very easy.
to create ui html is providing some html elements and tags.

drawbacks
----------
we can't reuse the Ui and time consuming to create ul.

2. javascript :-
ui------> javascript---------->predefined function------------>dom element------------>dom------------->screen
javascript we can create ui dynamically.
we can re-use UI.

drawbacks
----------
creating ui is very complex process and time consuming.

3. react js :-
ui------>react------>predefined function------------>react element------------>dom------------->screen


__________________________|___________________________________________|___________________________________________
|html                     |     javascript                            |    react js                              |
|_________________________|___________________________________________|__________________________________________|
|creating the UI is easy  |      we can reuse the ui                  |    creating ui is very easy like html    |
|we cannot resue the ui   |      creating the ui is complex process   |    we can reuse the Ui like js           |
|_________________________|___________________________________________|__________________________________________|